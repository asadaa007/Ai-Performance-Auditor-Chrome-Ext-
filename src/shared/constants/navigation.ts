/** Debounce window for rapid SPA route changes before notifying the background. */
export const NAVIGATION_DEBOUNCE_MS = 400;

/** When false, hash-only URL changes are ignored. */
export const NAVIGATION_TRACK_HASH_CHANGES = false;
